# Todo App (Flask)

A simple To-Do web app built with Flask and SQLite that supports:
- User registration & login
- Tasks with statuses: pending, working, done
- Add / update / delete tasks

## Tech Stack
Python, Flask, SQLite, HTML, CSS

## How to Run
1. Clone
2. `pip install -r requirements.txt`
3. `python run.py`
